{
  "name": "ai-antispam-landing",
  "version": "1.0.0",
  "description": "Landing page for AI Anti-Spam Bot",
  "scripts": {
    "build": "node build.js",
    "watch": "npx @tailwindcss/cli -i src/input.css -o dist/output.css --watch",
    "dev": "npm run watch",
    "preview": "node build.js --preview && concurrently -n watch,server \"node build.js --preview --watch\" \"(cd dist && python3 -m http.server 8080 & sleep 1 && open http://localhost:8080 && wait)\"",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "caniuse-lite": "^1.0.30001778",
    "concurrently": "^9.1.0",
    "fs-extra": "^11.2.0",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.2.1"
  }
}